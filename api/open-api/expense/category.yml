delete:
  summary: delete expense category
  tags:
    - Expense
  responses:
    200:
      description: delete category
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: null
    400:
      description: invalid params
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: string
                example: expense category didn't exists
